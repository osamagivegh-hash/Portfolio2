{
  "name": "portfolio-backend-deploy",
  "version": "1.0.0",
  "description": "Backend deployment package for Portfolio",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm start",
    "install-deps": "cd backend && npm install --production"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
